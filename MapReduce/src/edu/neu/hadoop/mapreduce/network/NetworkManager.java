package edu.neu.hadoop.mapreduce.network;



/**
 * Class used for handling network connections i.e. sending and
 * receiving data via {@link TextSocket}
 * 
 * @author Adib Alwani
 */
public class NetworkManager {
	
	/**
	 * Spawn Server thread to read incoming client connections
	 * 
	 * @param port Port Number to start server at
	 */
	public void spawnServer(int port) {
	}
}
